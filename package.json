{
  "name": "adblock-minus",
  "version": "0.4.1",
  "description": "Implements core functionality of adblocking",
  "main": "index.js",
  "engines": {
    "node": ">=6"
  },
  "scripts": {
    "test": "standard lib/*.js",
    "prebuild": "npm test && mkdir -p build && cp manifest.json build",
    "build": "browserify config/*.json lib/*.js -o build/bundle.js"
  },
  "keywords": [
    "adblock",
    "adblock-plus"
  ],
  "author": "xiaody",
  "license": "GPL-3.0",
  "repository": "xiaody/adblock-minus",
  "devDependencies": {
    "browserify": "13.1.0",
    "standard": "8.0.0"
  }
}
